server:
  port: 9001

dubbo:
  application:
    # 应用名称
    name: provider
  protocol:
    # 协议名称
    name: dubbo
    # 协议端口，默认 20880
    port: 20880
  registry:
    # 注册中心地址
    address: zookeeper://127.0.0.1:2181
